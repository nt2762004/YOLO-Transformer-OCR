# Notebook Material

This folder keeps the original notebook as tutorial and reference material.

- The notebook is the historical version of the pipeline.
- The modular code in `src/` is the maintained training and inference implementation.
- Use the notebook to understand the original experiment flow, then use `main.py` for reproducible runs.
